name: bidcdc_control
description: Windows and Android host application for the bidirectional buck-boost DCDC controller.
publish_to: "none"
version: 0.3.1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_libserialport: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
